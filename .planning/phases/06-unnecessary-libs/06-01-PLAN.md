# Phase 6: Unnecessary Library Loads

## Objective
Remove p5.js CDN from pages that don't use it. Audit all pages for unused script/CSS includes.

## Changes Made
- Removed `<script src="...p5.min.js">` from 9 sub-pages
- Removed orphaned `<div id="p5-canvas"></div>` from same 9 pages
- No page actually uses p5.js — the library was a leftover from a previous background effect replaced by the custom WebGL SDF shader on index.html

## Files Modified
cube-defence.html, dashing-simulator.html, isosceles-invasion.html, math-cubed.html, sketches.html, slime-slasher.html, snek-solver.html, sustain-a-city.html, turret-defence.html

## Audit Results
- No other unused scripts found
- All CSS includes are used (style.css on all pages, project-hero.css on project pages)
- index.html already had p5.js removed (comment at line 30)
- Dither-effect-example.html uses inline script only — no external libs
